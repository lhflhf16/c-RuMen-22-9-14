#define _CRT_SECURE_NO_WARNINGS 1

#include<stdio.h>
#include<string.h>

int main()
{
	int s = 1000;
	for (s = 1000; s <= 2000; s++)
	{
		if (s % 4 == 0 && s%100!=0)
		{
			printf("闰年%d ", s);
		}
		else if (s % 400 == 0)
		{
			printf("闰年%d ", s);
		}
		else
		{
			;
		}
	}
	return 0;
}
